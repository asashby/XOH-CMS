<div class="card-header">
    <h3 class="card-title">{{$title}}</h3>
    <a href="{{$url}}" style="max-width: 150px; float: right; display:inline-block;" class="{{$className}}">
        <i class="{{$icon}}"></i>
        {{$btn}}
    </a>
</div>
